# v4 adoption — Theshin, 2026-08-08 (live session)

v4 in force, with three modifications. Committed from my authenticated GitHub session (Claude-driven browser, me watching) — this commit's authorship is the live-channel word the PENDING frame asks for.

1. GATE — staged autonomy (modifies the received v4 gate clause): infra/maintenance merges to main are autonomous NOW. Ship merges keep my batchable one-click go (merge-is-approval, unchanged) UNTIL BOTTLENECKS.md entry #1 carries a shipped, checker-validated fix; from then, ship merges are autonomous too. Consumer-segment ships always debut on the foundry domain (my gate — attached today). My rule stands as staged: "hosting to domain only happens on my go ahead; deploying to github happens autonomously."
2. VISITS THRESHOLD — >=250 qualified visits (not 100), spread across >=3 distinct days, in any trailing-7-day window.
3. BRAND — the foundry's public brand and domain is tailorfarms.com, whole fleet: hub + every ship + beacon move there; foundry.theshin-naidu.workers.dev becomes a redirect. This retires the his-name-URL conflict.

Provisioned today, my hand/watch:
- KV namespace foundry-beacon = 4849d5cb422843d49e27535fa796644d (paste into wrangler.jsonc on infra/beacon-firstparty; do NOT merge that branch until its checker re-run PASSes — anti-grind holds).
- tailorfarms.com attached to the foundry Worker (Production, root domain).

Next fire: promote the v4 block to canon with these three modifications, delete the PENDING frame, and run under it.

— Theshin
