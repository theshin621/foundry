{
  "ships": [
    {
      "n": 1,
      "date": "2026-07-30",
      "slug": "hub",
      "one_liner": "the foundry's own hub — public ledger, factory library, and the deploy pipeline proven end-to-end",
      "gap_source_url": "PLAYBOOK.md §8 — ship #001 is the factory itself, proven on its own body before any market idea",
      "deploy_url": "/",
      "kind": "app",
      "checker_verdict": "PASS after one fix-cycle — initial PARTIAL (stale sites/ ship-path in 3 files; lib/ overclaim; verdict-before-merge) fully cured and re-verified; hub JS/stats/empty-states/mobile/secrets/PII verified against local cold fetch; live-URL unverified (egress)",
      "distribution": {
        "registry_pr": "n/a (not an MCP server)",
        "hub": "self",
        "x_draft": "drafted in briefs/2026-07-30.md"
      },
      "signal": {},
      "status": "live",
      "kill_criterion": "n/a — infrastructure ship; lives while the loop lives"
    },
    {
      "n": 2,
      "date": "2026-08-02",
      "slug": "gha-trigger",
      "one_liner": "paste a GitHub Actions workflow and an event, get WILL RUN / WON'T RUN plus the exact filter that decided it",
      "gap_source_url": "https://github.com/orgs/community/discussions/25285 — recurring 'my workflow didn't trigger' thread (plus community #164673, #180208, #185355). Occupant hunt found no simulator: rhysd/actionlint lints filter-pattern SYNTAX only and never evaluates a concrete event; nektos/act selects workflows by event name alone (its CLI skips the filter layer, though its pkg/workflowpattern library implements the semantics correctly).",
      "deploy_url": "unmerged: ship/002-gha-trigger @ 00bbaaa (checker FAIL - never staged, never deployed)",
      "kind": "app",
      "checker_verdict": "FAIL after one fix cycle (fresh checker, prior verdict did not carry over; primary checker on a DIFFERENT MODEL from the maker). Pass 1 primary, verbatim: \"VERDICT: PARTIAL - reported XSS is genuinely fixed and correctness is now excellent (56/56 cheat-sheet, 0/82+ inverted verdicts), but a new, undiscovered exponential-backtracking (ReDoS) bug in the glob compiler freezes the whole tab on ordinary pasted text (live-URL unverified (egress))\" - n=18 hangs >4000ms, n=40 >60000ms; render() is synchronous so the whole tab freezes and the try/catch never fires, leaving the previous verdict on screen looking authoritative. Pass 1 independent security refuter, verbatim: \"VERDICT: PASS - I tried hard (360+ executed payloads across every reachable sink, plus ReDoS and correctness sweeps) and could not execute any attacker-controlled JavaScript, exfiltrate anything, or force a wrong verdict.\" (it assessed the ReDoS as self-DoS, not attacker->victim - a recorded disagreement with the primary, not resolved in the ship's favour). Fix cycle: adjacent **/ runs collapsed (proved a semantic identity), a measured backtracking budget added, on:-as-list keys fixed, !-in-ignore-lists copy narrowed. Targeted re-check primary, verbatim: \"VERDICT: PARTIAL - the exact reported ReDoS shape is genuinely closed (proven equivalent + measured fast) and both non-blocking items are cleanly fixed with no regressions, but the new complexity budget still lets a different, narrower pattern shape hang the tab within its own stated limits - does not stage (live-URL unverified (egress))\" - pattern \"****a****b****c****d\" sits exactly at the cap, does not throw, and HANGS >10000ms. Fresh independent adversary on the fix diff, verbatim: \"VERDICT: FAIL (fix introduced a defect)\" - DEFECT 1 (HIGH): the budget throw is reachable from starGotcha's pattern-widening probe, so a correct WILL RUN verdict on legitimate input (branches: [\"x/y/z\", \"*?*?*/*\"], ref x/y/z) is replaced by an opaque TOOL ERROR citing \"**?**?**/**\", a string the user never typed; root cause: the quantifier branch never resets run. DEFECT 2 (HIGH): the budget's three dimensions are checked independently, so \"*****a+a+a+a+Z\" stays within all three and still takes 4577.8ms - and the adversary's own fairness note, verbatim: \"OLD code compiles the byte-identical regex for this pattern and takes the same ~1.1s at n=47 - this specific case isn't newly *slower*. What's new and false is the safety claim.\" VERIFIED SOUND across the re-check: XSS dead under ~440 executed payloads from two independent adversaries; 56/56 GitHub published cheat-sheet pairs; 0 inverted verdicts in 82+ scenarios; 21,435,888 exhaustive + 500,171 torture patterns and 66,953,290 (pattern,value) comparisons with ZERO unexplained divergences; 7/7 presets; 1 network request, 0 cookies, 0 storage; no prototype pollution; scout premise re-verified against actionlint and nektos/act source. Live-URL unverified (egress; no desktop bridge in a scheduled run).",
      "distribution": {
        "registry_pr": "n/a (not an MCP server)",
        "hub": "none — not merged, not on the hub",
        "x_draft": "drafted in briefs/2026-08-02.md"
      },
      "signal": {},
      "status": "failed",
      "kill_criterion": "<300 unique organic sessions in 30 days, or zero inbound links by day 30 -> kill to graveyard.md"
    }
  ]
}
