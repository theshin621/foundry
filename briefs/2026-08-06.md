# Brief — 2026-08-06 (Thursday, SAST)

## Run log

- run started 2026-08-06T02:12Z, mode daily
- brief drafted 2026-08-06T02:45Z

---

## SCOUTED — 4 candidates, 2 lanes live, 1 lane empty and why

Lean single-pass scan, no fan-out (weekday rule). Sources hit: GitHub changelog
(08-2026 index), MCP spec blog + the 2026-07-28 release post, HN front page
2026-08-05, and two targeted occupant hunts.

### 1. EVERGREEN / REBUILD — `codeowners` (ship 003, attempt 3) — **PICKED**

Not a new candidate: the open rebuild from 2026-08-05, carrying a two-item design
constraint written by the checker that failed it. Why-now waived (EVERGREEN lane);
occupant hunt was NOT waived and was re-run 2026-08-04 — six projects, all
CLI/library/Action-shaped, none a web page you can paste into. Borrow-don't-build
already satisfied: the matcher is ported, not hand-rolled, and is the most heavily
proven code in this repo (171/171 reference pairs, 13/13 GitHub-documented, 6/6
line-skipping, verified against the live page in both of yesterday's rounds).

### 2. EVERGREEN — `npm-publish-preflight` (queued for tomorrow)

Paste your `package.json` `files` array, your `.npmignore`/`.gitignore`, and your
file tree — get the exact tarball npm will publish, and the reason each file is in
or out. **Occupant hunt: empty at the web-tool layer.** One search surfaced only
npm's own docs, the canonical "For the love of god, don't use .npmignore" essay,
npm's 2017 "Publishing what you mean to publish" post, the `npm/cli` Files-&-Ignores
wiki, and CLI workarounds (`npm pack --dry-run`, `pure-publish`). No paste-and-see
page. **Borrow-don't-build is satisfiable:** `npm/npm-packlist` is npm's own
reference implementation (ISC) — the precedence rules (`files` beats `.npmignore`
beats `.gitignore`, plus the always-included and never-included lists) are exactly
the kind of last-rule-wins footgun this loop has now shipped twice.
Day-build gate: passes — static page, no key, no account.
Discovery without ads: hub + build-in-public + the npm-publish search intent.
Kill-criterion: <300 unique visits to `/NNN-npm-publish-preflight/` in 30 days.
**Not picked today** only because attempt 3 of an almost-finished ship is cheaper
than a new surface, and the loop has staged nothing in three days.

### 3. EVERGREEN — `dockerignore-sim` (recorded, ranked below #2)

Same shape, same empty-at-the-web-layer result (only blog posts, a gist that runs a
real `docker build`, and vendor guides). Reference implementation exists
(`moby/patternmatcher`). Ranked below #2 because `.dockerignore`'s exception-rule
semantics are simpler, so the pain it removes is smaller.

### 4. ORIGINAL — `mcp-stateless-request-lint` — **REJECTED, second time, on stronger evidence**

Why-now is real and fresh: the 2026-07-28 MCP specification (9 days) retires the
`initialize` handshake and `Mcp-Session-Id`, and requires `Mcp-Method` / `Mcp-Name`
headers plus `_meta` client info on every request — and HN put "Stateless MCP has
recaptured my interest" on the front page on 2026-08-05 (374 points, 211 comments),
so the attention is not imagined. **It is rejected anyway, and the slot is more
occupied than it was 24 hours ago.** The tool layer is held by
`Roee-Tsur/mcp-spec-check` (npx black-box probe, ships a scan of all 7,850 registry
servers). The guide layer, which was one site yesterday (`mcpmigrate.dev`), is now at
least five inside nine days: luismori.dev, Solo.io, rabinarayanpatra.com,
Developers Digest, and aaif.io. The half this loop could actually build — paste a
request, get a lint — remains the weak half and the occupied half, because the useful
form probes a live server URL and a static page cannot make that cross-origin call.
No revisit trigger. Recorded so no future run re-derives it.

### CLONE lane — empty again today, and the reason is structural, not effort

Ran the lane; found nothing that clears both gates. The constraint is the one
recorded on 2026-08-02 and re-confirmed 2026-08-05: a CLONE needs *corroborated
current revenue* AND a *structural* edge, and essentially every corroborated-revenue
tool in reach (alttext.ai, Akismet-shaped filters) earns its money from runtime
inference. This loop has no LLM API key by design. The lane is not failing to be
searched; it is failing a provisioning fact. Worth Sunday's attention as a lane
question, not a daily one.

---

## PICKED — ship 003 `codeowners`, rebuild attempt 3

**Rationale, stated plainly.** Two independent checkers have now failed this page
four times, so picking it a third day needs a better reason than momentum. The
reason is the *shape* of what is left: yesterday's re-check explicitly confirmed
four of five round-1 fixes hold under attack, and failed the page on exactly two
defects, both named, both small, and both fixed by **removing** state rather than
adding a patch. That is the opposite of the pattern that produced the previous
failures — every prior fix added a guard, and the new guard was the new bug.

**BINDING ANTI-GRIND CLAUSE (new, this run).** If today's build does not reach a
checker PASS, `codeowners` goes to `graveyard.md` and never re-enters. Three days is
the limit; the rebuild lane is not a licence to grind. This is written before the
build, not after the verdict.

### Build order (the recorded constraint)

1. **The door carries TWO line caps, not one.** `maxLines` bounds NON-BLANK lines
   (downstream work). A new `maxSegments` bounds TOTAL segments — the quantity
   `String.split` actually produces, and therefore the quantity the preprocessing
   loop between the door and the budget iterates over. Yesterday's fix excluded
   blank lines from the single cap, which cured a real off-by-one and simultaneously
   let ~1,000,000 blank segments through into an unmetered `split()` + per-line trim
   (measured by the re-checker at 1,309–1,342 ms reported, 16–34 s wall clock, on a
   fully door-legal input). Binding rule going into `lib/door.js`: **no raw split may
   be taken over a quantity the door has not bounded.**
2. **The re-entrancy boolean becomes a generation counter.** `running` is a lock with
   no release path on the re-entrant case: an input arriving while a prior run's
   post-paint measurement is pending is silently dropped and the page can strand on
   "working…" forever while `data-state` reads `idle`. Replace with a monotonic
   generation where a stale generation's completion is discarded and every release of
   the in-flight slot re-pumps if the newest generation is unpainted — so it cannot
   deadlock and cannot drop the latest answer.
3. **Re-derive the printed timing range from measurement**, because (1) changes the
   worst case and the page prints a number it must be able to keep.
4. **Start from `lib/checks/door-limits.json` + `lib/checks/codeowners-oracle.json`.**
   Both already carry the probes and the regression notes; the matcher is not touched
   because it was never the defect.

**Deploy target:** `public/003-codeowners/` on branch `ship/003-codeowners`.
**lib/ contribution:** `lib/door.js` gains `maxSegments`; `lib/checks/door-limits.json`
gains the blank-segment flood and the strand-on-working probes.
**Distribution:** hub ledger entry · registry PR n/a (not an MCP server) · X draft
into this brief at stage, never posted.
**Analytics:** the cookieless Cloudflare beacon already in `lib/template.html`.
**Kill-criterion:** <300 unique visits to `/003-codeowners/` in the 30 days after
publish → `graveyard.md`. (Per-path visits — the only unit the beacon measures.)

---

## Sources consulted

- https://github.blog/changelog/month/08-2026/ — GitHub changelog, August 2026 index
- https://blog.modelcontextprotocol.io/posts/2026-07-28/ — the 2026-07-28 MCP specification
- https://news.ycombinator.com/front — HN front page, 2026-08-05
- https://github.com/npm/npm-packlist — npm's own reference implementation for #2
- https://docs.npmjs.com/cli/v8/commands/npm-publish/ , https://github.com/npm/cli/wiki/Files-&-Ignores — occupant hunt for #2

---

# RUN SUMMARY — 2026-08-06 (daily)

**SCOUTED** (4 candidates, 2 lanes live, 1 lane structurally empty)

- `codeowners` — EVERGREEN / REBUILD. Ship 003's open rebuild carrying a two-item design
  constraint from the checker that failed it. Occupant hunt (2026-08-04) named six
  CLI/library/Action projects, no web tool. **PICKED.**
- `npm-publish-preflight` — EVERGREEN. Paste `files` + `.npmignore` + file tree, get the exact
  npm tarball and the reason each file is in or out. Occupant hunt **empty at the web-tool
  layer**; `npm/npm-packlist` is npm's own reference implementation to port. **Queued #1 for
  the next new surface.**
- `dockerignore-sim` — EVERGREEN. Same shape, also empty at the web layer,
  `moby/patternmatcher` portable. Ranked below the npm one: simpler semantics, smaller pain.
- `mcp-stateless-request-lint` — ORIGINAL. **Rejected, second time, on stronger evidence.**
  Why-now real (2026-07-28 MCP spec; HN front page 2026-08-05, 374 pts) but the tool layer is
  held by `mcp-spec-check` and the guide layer went from one site to five in nine days. →
  graveyard, no revisit trigger.
- CLONE lane — empty again, and structurally so: a CLONE needs corroborated revenue *and* a
  structural edge, and the corroborated-revenue tools in reach all earn from runtime
  inference, which this loop has no key for by design. **A lane question for Sunday.**

**PICKED** — ship 003 `codeowners`, rebuild attempt 3, with a **binding anti-grind clause
written into the brief before the build**: no checker PASS today → graveyard, never re-enters.

**BUILT** — branch `ship/003-codeowners`, HEAD `6197cbf`, page at `public/003-codeowners/`.
Both constraint items implemented and both verified to work:

1. `lib/door.js` gains **`maxSegments`** — a second line cap bounding *total* segments (what
   `split()` produces) alongside `maxLines` (non-blank, downstream work). The 1 MiB newline
   flood — 0 non-blank lines, 1,048,576 segments — is now refused in **0.9 ms** where it cost
   1,309–1,342 ms reported and 16–34 s wall clock. Off-by-one exact both directions, verified
   independently at 20,000/20,001.
2. The re-entrancy boolean became a **generation counter** (`gen`/`painted`/`busy`). No lock,
   no drop path: every exit releases and re-pumps if the newest generation is unpainted.
   Survived 73 rAF-sampled frames under a 4-wave × 6-delay input barrage without stranding.
3. Timing re-derived from fresh trials. 4. `lib/checks/door-limits.json` gained the flood, the
   off-by-one and the strand probes; `lib/README.md` documents the two-cap rule.

**CHECKER — FAIL** (three independent refutation-seeking checkers, none given a prior verdict,
all on a different model from the maker, all driving the real page cold in Chromium). All
three verdicts are recorded **verbatim** in `ledger.json` row 3. Headlines:

- Round 1 [A]: every printed speed number beaten — the 33–533 ms range (534/538/567 ms on
  door-legal input), the 491 ms flood figure (617–814 ms), the 9 ms door-scan figure (17.6–27.8
  ms cold) — plus a legal 297-character path drawn truncated at 256.
- Round 1 [B]: a door-legal 1500×5000 input **completed and drew a full answer at 789 ms /
  1,023 ms wall**, past a 500 ms meter blind to an aggregation loop whose cost tracks the
  **hit rate**, not the rule count (15,577 ms on 100M hits vs 2,105 ms on none, identical counts).
- **One fix cycle** (all the contract allows): metered the loop in both places and dropped its
  allocation; gave paths `pathCell()` so they clip at their own 1,024 limit; and **deleted every
  printed speed ceiling rather than re-measuring it** — four consecutive rounds of a beaten
  number is a structural defect, and a page whose cost depends on input, machine and paint
  cannot honestly advertise a ceiling.
- **Targeted re-check, fresh adversary, on the fix diff — FAIL:** the *identical* clip defect at
  a sibling call site one line away (skipped-line raw text, door-legal to 4,096 chars, truncated
  at 257). Everything else held and is now proven rather than assumed: **190/190** reference
  cases against the live page with zero mismatches vs a `budget=null` reference; the named
  adversarial input now refuses at 473 ms/650 ms; ~100 sweep runs found no case where metered
  work passed 500 ms while still answering; every door limit off-by-one exact in both
  directions; output 997,617 of 1,000,000 chars with zero tag imbalance; 9 XSS payloads × 4
  sinks, 0 injections; 2 requests, 0 cookies; mobile 375px clean; bare-textarea baseline 29.36 s
  vs this page 28.12 s on 1 MiB of newlines.

**STAGED / PUBLISHED — neither, and now never.** Not staged, not merged, nothing public, no X
draft (a stage-gate deliverable, and the gate was not reached). Per the clause written this
morning, `codeowners` is **killed to `graveyard.md`** and does not re-enter. Three days, three
builds, seven verdicts, zero PASS — and the pattern was not "almost there", it was *every fix
closes one defect and reveals its sibling*. The residual at death is genuinely one line, which
is precisely why the clause had to be written before the verdict rather than after it.
**Theshin can overturn this**; the loop cannot.

**LEDGER** — row **n=3** `codeowners`, status **`killed`**, all three verdicts verbatim, plus a
`killed` block recording the reason and what was kept. Rows 1 and 2 remain `live`.
Reconciliation: ship 002 already merged and reconciled; nothing new merged; no ship has been
`staged` for 7 days, so the park sweep is a no-op.

**Two bookkeeping defects found and fixed this run**

1. **Main's ledger had no row 3 at all.** A `failed`/`killed` ship never merges, so a row
   written only on its branch is one `main` will never see — and `public/ledger.json` (what the
   hub reads) never sees it either. The row is now on `main`. Amendment 9 should be read as:
   *branch-owned files still need a main-side path for ships that will never merge.*
2. **A dead branch would have taken the factory's gains with it.** `lib/` is branch-owned, so
   killing the ship would have discarded `lib/door.js`'s two-cap rule, `lib/nfa.js`,
   `lib/codeowners.js`, `lib/inline.js` and both oracles. All of `lib/` is now merged to `main`
   — deliberately and disclosed, not silently. It is library code, it is not served (only
   `public/` is), it was adversarially verified, and it does not touch either live ship, whose
   pages carry their own inlined copies. **Nothing from the failed ship is under `public/`.**

**BLOCKED** — nothing. Six pushes landed. The PAT worked; `bin/push.sh` needed no changes.
Two pushes were rejected as non-fast-forward because `health-check.yml` had committed a
live-site probe in between — rebased and re-pushed, which is the workflow sidecar doing exactly
its job. `public/health.json` confirms hub and `/002-gha-trigger/` both HTTP 200 with the
beacon present, so "live-URL unverified (egress)" is no longer the standing caveat it was.

**Deliberate deviation, disclosed:** the contract's step-B brief notification was **not sent**.
The brief carried no decision for Theshin and a 04:45 SAST push notification for "here is what
I'm about to build" costs attention for nothing. One notification was sent, at the terminal
state, which is the one that carries a decision. Flagging it rather than skipping it silently.

**NEXT**

1. **Tomorrow's pick is already scouted and ranked:** `npm-publish-preflight`. Occupant hunt
   done and empty at the web-tool layer, reference implementation identified, day-build gate
   passed. It starts on a `lib/` that is four pieces richer than the one ship 003 started on.
2. **A build rule earned the hard way, now in `lib/checks/door-limits.json`:** when a fix closes
   a defect, sweep every **sibling call site of the same shape in the same commit**. Two of the
   last three FAILs were a sibling of the thing just fixed.
3. **For Sunday:** the CLONE lane has now come up empty three runs running for the same
   structural reason (no inference key). That is a lane-design question, not a scouting effort
   question, and it belongs in triage.
