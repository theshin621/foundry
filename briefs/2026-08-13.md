# 2026-08-13 — daily fire (Thursday)

run started 2026-08-13T02:11Z, mode daily, v4

**Gate read at run start (`BOTTLENECKS.md` entry #1):** status `fix-shipped`, **checker-validated:
PENDING**. Therefore SHIP merges require Theshin's one-click go this fire; INFRA/MAINTENANCE merges
are autonomous on checker-PASS. **Build days are NOT blocked** — no cause sits at count ≥2 without a
shipped fix (entry #1 `fix-shipped`, entry #2 `fix-shipped`). Clock day **4 of 30** (clock-start
`2026-08-09T14:52:09Z`, day 30 = `2026-09-08`).

---

## 0. Instrument read at run start (measured, not claimed)

The heartbeat push triggered `beacon-stats.yml`; the committed artifact refreshed at
`2026-08-13T02:12:45Z`, oracle verdict **PASS**
(provenance: [Actions run 31660120238](https://github.com/theshin621/foundry/actions/runs/31660120238)).

| path | 08-09 | 08-10 | 08-11 | 08-12 | trailing total |
|---|---|---|---|---|---|
| `/` (hub) | 13 | 14 | 45 | 9 | **81 over 4 distinct days** |
| `/002-gha-trigger/` | – | – | – | – | **0** |
| `/004-khanya-school-tutor/` | – | – | – | – | **0** |
| `/005-maccleaner/` | – | – | – | – | **0** |

The hub's 45-visit spike did not repeat (45 → 9). Every ship remains exactly zero across the whole
10-day window. MOD-2's threshold (≥250 on **one ship**, ≥3 distinct days, trailing-7) is not
approached on any ship, and the hub does not count toward it.

---

## 1. The day's first act was to try to falsify that zero — and it survived

Ten days of steering data say *nobody uses the ships*. Before scouting anything, this fire asked the
only question that could invalidate that reading: **has the loop ever confirmed, in a real browser,
that a ship page's beacon actually fires?** It had not. `public/health.json` reports
`has_beacon: true` for all four paths, but that field is a substring test (below), and BOTTLENECKS #1
incident #4 is precisely *"the inlined beacon was inert on all five pages while every page still
returned HTTP 200"*. A phantom zero and an honest zero are byte-identical in the artifact.

**Method.** Served `public/` locally (`python3 -m http.server`) and executed all four pages in
Chromium via Playwright, listening for a network request to `/_b`.

**Round 1 result: 0 beacon attempts on all four pages — an apparent catastrophe.** It was not.
The snippet's first line is `if(navigator.webdriver) return;` — the bot guard, working exactly as
designed, and it suppressed the very measurement being taken.

**Round 2, with `navigator.webdriver` neutralised** (`addInitScript` + `--disable-blink-features=AutomationControlled`):

| path | HTTP | `navigator.webdriver` | beacon attempts | page errors |
|---|---|---|---|---|
| `/` | 200 | false | **`POST /_b`** | none |
| `/002-gha-trigger/` | 200 | false | **`POST /_b`** | none |
| `/004-khanya-school-tutor/` | 200 | false | **`POST /_b`** | none |
| `/005-maccleaner/` | 200 | false | **`POST /_b`** | none |

**The beacon is live on every page. The zeros are real demand data.** That is the most useful thing
this fire can tell Theshin, and it is the opposite of what it set out looking for. The 30-day
review's evidence base is sound; the fleet's problem is uptake, not instrumentation.

**But the probe surfaced a real defect on the way past, and it is today's build.**

---

## 2. The defect: `health.json` asserts a browser behaviour and measures a substring

`.github/workflows/health-check.yml` derives the field as:

```python
return {..., 'has_beacon': "'/_b'" in body}
```

A raw substring test on the response bytes. Under BOTTLENECKS #1's third clause — *"for
liveness-of-markup claims the oracle executes the page in a real browser and observes the behaviour
itself; hand-written parse-and-match walkers are FORBIDDEN"* — this field is not permitted to exist
in its current form. It reports `true` for a snippet sitting inside `<template>`, `<noscript>`, an
HTML comment, or a `<script type="application/json">`: every vector incidents #4 and #009 already
paid for, all four of which leave the string `'/_b'` in `body` and the beacon dead.

This is not hypothetical for this field. Its own inline comment records that it *already failed this
exact way once* — until 2026-08-10 it matched `cloudflareinsights.com`, a third-party beacon ship
008 had removed, so **every page reported `has_beacon: true` while the field measured a script the
loop no longer depended on.** The string was changed; the method that made the lie possible was not.

And today's round-1 result proves the naive replacement is also wrong: a browser-truth oracle that
does *not* neutralise `navigator.webdriver` returns a confident **false RED** on a perfectly live
beacon. Both failure directions are now empirically demonstrated, before a line of code exists.

---

## 3. SCOUT — three lanes, lean weekday scan

**ORIGINAL — empty (9th consecutive run).** No dated hook inside the 14-day window
(≥2026-07-30) survived the day-build gate. The August API-deprecation cluster
([OpenAI deprecations](https://developers.openai.com/api/docs/deprecations),
[Adobe Photoshop API](https://developer.adobe.com/firefly-services/docs/photoshop/getting-started/deprecation-announcement/),
[Zuora end-of-support](https://docs.zuora.com/release-notes/end-of-support)) concerns hosted
proprietary endpoints — **no local artifact a static page could parse**, the same disqualifier that
killed the Copilot and Spark hooks yesterday. Nine empty runs is itself a finding and is carried to
Sunday triage as a lane-viability question.

**CLONE — empty (9th consecutive run).** Nothing corroborated on current revenue *and* holding a
structural edge. No runtime-inference builds are possible by design (no LLM API key), which removes
the category where most 2026 clone edges live.

**EVERGREEN — one killed today, two carried.**

- **New, and killed on occupancy: `srt-offset-resync`.** Occupant hunt performed, not assumed:
  page 1 returns **ten** free no-signup browser-side resync tools
  ([Subtitle Toolbox](https://khangyen.github.io/subtitle-toolbox/),
  [SubShifter](https://subshifter.bitsnbites.eu/),
  [HappyScribe](https://www.happyscribe.com/tools/srt-time-shift),
  [subtitlekit](https://subtitlekit.com/en/resync-subtitles/), et al.), several already
  "no upload". There is no structural edge left to take. Killed at scout, not carried.
- **Carried, unbuilt: `password-csv-remapper`** (yesterday's E1). Still the strongest consumer
  candidate in the backlog — the promo-motive check is structural (nobody markets migration *away*
  from their own product) and "never leaves your browser" is the entire product for a plaintext
  password file, which disqualifies any server-side competitor by architecture.
- **Carried, unbuilt: `ics-conflict-finder`** (yesterday's E2). Soft gap adjacent to a crowded
  category; ranked second, honestly weak.

Dedupe: all three checked against `ledger.json`, `graveyard.md`, `candidates-seen.json`.

---

## 4. RECOMMENDATION — `infra/014-beacon-liveness-oracle`

Not a consumer ship, and the brief owes the argument. Four reasons, descending:

**(a) It is the only thing that can publish today.** Entry #1 is checker-validated PENDING, so MOD-1
holds SHIP merges on Theshin's go. `password-csv-remapper` built today *stages and waits*; this
merges on a PASS.

**(b) The evidence for it was produced today, empirically, in both directions.** The negative control
and the independent break are not designed — they are measured facts already in hand: the substring
method passes a dead beacon (#4, #009 vectors), and the naive browser method fails a live one
(`navigator.webdriver`, observed this morning). Probe-the-oracle normally comes after the oracle;
here it came first.

**(c) It retires a forbidden construct rather than repairing it**, which is the shipped fix's actual
instruction — *"the hand-written parse-and-match band is deleted, not repaired"*.

**(d) The shape is one entry #1 can converge on.** The entry's own open question #2 asks whether the
cause is upstream at *pick* time, since #006/#007/#012 were all "port a long-tailed reference
implementation's semantics into a browser page". This is not that shape. The target set is one
binary property per page — *did a POST to `/_b` occur* — fixed and tiny before the first line.

**Deliberately NOT recommended: a third attempt at `infra/013-stats-window-floor`.** It has two
independent FAILs and a rising severe count. A third round on the same artifact is the grinding the
anti-grind clause exists to stop. Its underlying defect (`HARD_CAP = 44`; the window silently shrinks
as the fleet grows and blacks out entirely past 44 paths) is **not urgent at 4 countable paths** —
today's window is 10 days, comfortably above the trailing-7 the stop-condition needs — and it is
re-filed as decision-debt, not re-attempted.

### Build order

1. **ARCHITECT.** `oracles/beacon-liveness/oracle.py` before any product code. Predicates, cold and
   executable: for each live ship path (read from `ledger.json`, infra rows excluded) the page is
   loaded in Chromium with `navigator.webdriver` neutralised, and a request to `/_b` MUST be
   observed. Verdicts PASS / FAIL / CANNOT-CERTIFY (no browser → decline, never bless).
2. **PROBE-THE-ORACLE (mandatory).** (a) Negative control — the four documented inert vectors
   (`<template>`, `<noscript>`, HTML comment, `type="application/json"`) each MUST go RED, plus
   snippet-absent. (b) Independent break attempt — construct a page the oracle passes while the
   beacon is dead, and a live page it wrongly fails.
3. **BUILDER.** Replace `has_beacon` in `health-check.yml` with a browser-executed measurement, or —
   if the runner cost is not justified — remove the field outright rather than keep a forbidden
   claim in a published artifact. Borrow-don't-build: Playwright drives; the hand-written band stays
   minimal, per entry #1's *"a borrowed primitive does not make the caller correct"*.
4. **CHECKER.** Independent subagent, different model, driven cold against the architect's oracle.
