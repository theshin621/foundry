# Brief — 2026-08-11 (Tuesday) — day 012

run started 2026-08-11T02:10:50Z, mode daily, v4


## 1 — SETUP + HEARTBEAT

- Repo cloned fresh (`94d5423`), `config.json` written with the rotated PAT, heartbeat pushed
  `7c16657` at 02:11Z. Push path verified working on the first try.
- **Build-day rule, read against HEAD before anything else:** `BOTTLENECKS.md` entry #1 is
  `status: fix-shipped`; entry #2 is `fix-shipped`. **No cause at count ≥2 is unshipped, so today is
  a build day.**
- **Ship-merge gate:** entry #1 still reads **`Checker-validated: pending`** — the clause requires a
  clean ship-shaped **PASS** and the loop has not produced one. So under MOD-1, **ship merges remain
  Theshin's one-click go**; infra/maintenance merges are autonomous on checker-PASS. This fire does
  not argue itself past that gate.
- **Pending releases:** ledger row 11 `diffusion-curves` is `staged` as of 2026-08-10 — **1 day old**,
  well inside the 7-day park window. Gate applies: stays staged, awaiting `go 11`.

## 2 — SCOUT (lean weekday, 4 searches, 0 subagents)

**Frame.** The pick was not open this morning. On 2026-08-10 Theshin handed the loop an explicit
ordered queue over the mechanism-card deck (`"1-5 , now"`): card 1 built and staged yesterday as ship
011, **card 2 next**. The loop's own 2026-08-10 scout independently landed on the same card as its
carried recommendation (`candidates-seen.json`: *"chat-export-redactor — RECOMMENDED, carried to the
next build day"*). Human order and machine recommendation agree, so this fire spent its scan budget
on **falsifying** the pick rather than re-deriving it.

### Candidates

**C-1 · MESSAGING · Chat Export Redactor** — card 2. Drop a WhatsApp `.txt`/`.zip` or Telegram
`result.json` into a static page; every participant gets a consistent pseudonym, phone numbers,
emails, vCards and links are stripped, and you download the sanitised export plus a mapping file that
never leaves the machine. Framing: *share a chat with an LLM, a lawyer or a researcher without
doxxing everyone in it.*

- **Why-now** — waived-adjacent, but real: the demand phrase is *"share whatsapp chat with chatgpt
  safely"*, which did not exist as a use case two years ago. Provenance is academic and dated:
  Utrecht D3I / Boeschoten et al. (CCR 2022) and Port (JOSS 2023) — the "process the data-download
  package entirely client-side" pattern.
- **Occupant hunt — re-run this fire, and it is the second independent run on this card.** Search
  returned: `printchat.app` (ships redaction, **server-side and paid**), `WhatsR` (CRAN R package —
  anonymises, but it is an R package for researchers, not a browser), `waexport.wadesk.io` and the
  WAexport Chrome extension (**export**, not redact), `cleanor.app` screenshot redactor (images, not
  transcripts), `whatsapp-chat-analyze` on PyPI (stats). **Nothing free, browser-side and
  chat-format-aware.** Niche is THIN, not empty — the honest word, unchanged from 2026-08-10.
- **Structural edge** — architectural, not inferential: every occupant either takes your transcript
  onto their server, or is a CLI/R package a normal person will not run. This runs in the tab and the
  file never leaves. That is exactly the harm the tool exists to prevent, so the incumbent's
  architecture *is* the wedge. No LLM key needed by construction.
- **Day-build gate** — PASS. Static page, no accounts, no review, no marketplace, no runtime
  credential. The risky core (timestamp-format parsing) is bounded and the oracle is
  **self-verifying**: rescan the output for every identifier extracted from the input; the answer must
  be 0 hits.
- **Kill-criterion (beacon-denominated, per Amendment 2026-08-02b.5 — the card's own
  "<30 files processed" is NOT instrument-measurable and is rejected):**
  **`/012-chat-export-redactor/` under 40 qualified visits in any trailing 14-day window after one
  discovery post → kill.**

**C-2 · MEDIA/AI · Train/test contamination + exact-dedup scanner** — card 3, Theshin's #3.
Suffix-array scan of `train.jsonl` × `test.jsonl` for shared n-grams with byte offsets. Occupancy:
CLIs exist (google-research dedup, semhash); the browser-side checker is empty. Oracle is unusually
strong — every finding is re-checkable by independent string search at the reported offsets.
**Not picked:** it is behind C-1 in Theshin's stated order and the audience is narrower (ML
practitioners already comfortable with a CLI, which is the occupant).

**C-3 · GAMING · GLB interpenetration/tunneling auditor** — card 4, Theshin's #4. Ferguson's
Tight-Inclusion CCD (MIT) compiled to WASM, run over an animated GLB, exact clipping pairs
highlighted. **Not picked:** the card names its own risk — *"the WASM port is the day-build stretch"*
— and yesterday's fire already burned a re-scope on a day-build gate misjudgement (WebGPU → WebGL2).
Two implementation-risk stretches in three days is how a loop stops shipping.

### Dedupe

None of C-1/C-2/C-3 appears in `ledger.json` rows 1–11 or `graveyard.md`. C-1 is in
`candidates-seen.json` as *recommended, carried* — the intended state, not a duplicate.

### Standing question — *"what becomes possible at ~zero build cost that has no name yet"*

The honest answer for C-1: nothing new becomes *possible*. What changes is who can do it. Redaction
of a chat log has been possible for years for anyone who will run a Python CLI. The zero-build-cost
move is that the same transform now runs in a tab for someone who will not. That is a distribution
change wearing a product's clothes — worth naming plainly rather than dressing up as a category.

**PICK: C-1 · ship 012 · chat-export-redactor.**
