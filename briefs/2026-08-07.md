# Brief — 2026-08-07 (Friday, SAST) · day 006

**run started 2026-08-07T02:11:43Z, mode daily**


---

## SCOUT — lean daily pass (single, no fan-out), 2026-08-07

Sources swept: Claude Platform release notes (vendor changelog) · MCP blog + registry coverage ·
one news sweep · builder-chatter sweep · four targeted occupant hunts. Six candidates screened,
three viable, three rejected with the evidence recorded.

### 1. `npm-publish-preflight` — EVERGREEN — **RECOMMENDED**

**What:** paste your `package.json`, your `.npmignore`/`.gitignore` and your file list; see the
exact set of files `npm publish` will put in the tarball, and for every file the *one rule that
decided it* — which source, which pattern, which line.

**Why it exists:** the precedence between `files[]`, `.npmignore`, `.gitignore` and npm's own
always-in/always-out lists is genuinely confusing, and the failure is silent and public: people
ship `.env` and `node_modules`, or publish a package with `dist/` missing, and only find out
after the version is immutable on the registry.

**Occupant hunt — run fresh today, three searches, empty at the web-tool layer.** What exists is
documentation and CLI: npm's own docs, the npm/cli *Files & Ignores* wiki, npm's 2017
"Publishing what you mean to publish" post, the "don't use .npmignore" essay, and CLI-only
workarounds (`npm pack --dry-run`, `pure-publish`, the `npmignore` package). No page takes the
four inputs and shows you the resolved set with per-file reasons. `npm pack --dry-run` is the
closest thing and it (a) needs the project checked out locally, (b) gives you a file list with
no *why*, and (c) is exactly the step people skip.

**Structural edge:** whole-resolution vs one-rule, and *reasons* vs a bare list. No signup, no
install, nothing leaves the browser — which matters, because the input is your private tree.

**Promo motive:** none. No vendor benefits from me believing this; the sources are npm's own docs
and a nine-year-old complaint thread.

**Day-build gate: PASS.** Static page, no API key, no account, no marketplace, no review.
Borrow-don't-build satisfied twice over: the *semantics* are published rules (three citations
below) and the *matcher* already exists in this repo — `lib/nfa.js`, proven linear-time.

**Rules pinned to primary sources (the checker re-verifies these, not my memory):**

- Always included, any case/extension: `package.json`, `README`, `LICENSE`/`LICENCE`, the file
  in `main`, the file(s) in `bin`.
  — <https://docs.npmjs.com/cli/v11/configuring-npm/package-json>
- Always ignored: `*.orig`, `.*.swp`, `.DS_Store`, `._*`, `.git`, `.hg`, `.lock-wscript`,
  `.npmrc`, `.svn`, `.wafpickle-N`, `CVS`, `config.gypi`, `node_modules`, `npm-debug.log`,
  `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`, `bun.lockb`.
  Of those, a hard core **cannot be re-included even if named explicitly**: `.git`, `.npmrc`,
  `node_modules`, `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`, `bun.lockb`.
  — same page.
- `.npmignore` **replaces** `.gitignore`, it does not merge with it: "If there is a `.gitignore`
  file, and `.npmignore` is missing, `.gitignore`'s contents will be used *instead*." An **empty**
  `.npmignore` therefore turns `.gitignore` off entirely.
  — <https://docs.npmjs.com/cli/v11/using-npm/developers/> and
    <https://github.com/npm/cli/wiki/Files-&-Ignores>
- `files[]` is an allowlist and currently **wins over** `.npmignore`: "Entries matched by `files`
  will be included, regardless of `.npmignore` settings" — flagged in npm's own wiki as a known
  issue (#11669) rather than intended design. The page states this *and* labels it as npm's open
  bug, because a tool that quietly picks the "right" answer over the shipped one is lying.
  — <https://github.com/npm/cli/wiki/Files-&-Ignores>

**Kill-criterion:** fewer than **300 per-path visits to `/006-npm-publish-preflight/` in the 30
days after publish** → `graveyard.md`. (Per-path pageviews are the only unit the cookieless
beacon measures — Amendment 2026-08-02b clause 5.)

### 2. `dockerignore-sim` — EVERGREEN — viable, ranked #2

Paste a `.dockerignore` and a file list, see the real build context. Occupant hunt empty again
today at the web-tool layer (blog posts, a gist that shells out to a real `docker build`, vendor
guides, a Read-the-Docs builder reference). Reference implementation exists (`moby/patternmatcher`).
Ranked below #1 for the same reason as yesterday: `.dockerignore`'s exception semantics are
simpler, so the pain removed is smaller — and the secret-leak consequence is a slow build rather
than an immutable public publish.

### 3. `rsync-filter-sim` — EVERGREEN — new today, viable, ranked #3

Paste `--include`/`--exclude`/`--filter` rules and a tree; see what actually transfers. Occupant
hunt run today: **empty at the tool layer** — seven results, all prose (Linuxize, phoenixNAP,
Baeldung, monovm, two linuxvox how-tos, 0ink.net's rules explainer). The volume of explainer
posts is itself the demand signal: rsync's first-match-wins ordering with directory traversal is
the single most-asked-about thing about the tool. Ranked #3 only because the semantics are the
hairiest of the three (per-directory traversal + `/***` idioms), i.e. the least day-*verifiable*.

### 4. `llm-deprecation-preflight` — ORIGINAL — **REJECTED, occupied**

Why-now was real and fresh: **2026-08-05, two days old — `claude-opus-4-1-20250805` retired, all
requests to it now error** (<https://platform.claude.com/docs/en/release-notes/overview>).
Rejected on occupancy, and not marginally: `llmstatus.ai`, `presenc.ai/research/ai-model-deprecation-tracker-2026`,
`benchr.org/deprecations`, `hidekazu-konishi.com`'s lifecycle calendar, `quora/model-deprecation-tracker`
on GitHub, and an *LLM Model Deprecation Check* action already on the GitHub Marketplace. Six
named occupants across three layers (site, library, CI action). `llm-status-aggregator` was
already in `candidates-seen.dropped_on_occupancy` — this is the same slot from a different angle,
and it is more occupied than it was.

### 5. `anthropic-request-preflight` — ORIGINAL — **REJECTED, the incumbent is the API itself**

Paste a Messages API request, get told which current rules it breaks — the 2026-07-24 change
making `thinking:{"type":"disabled"}` a 400 at effort `xhigh`/`max`, `speed:"fast"` erroring on
`claude-opus-4-7`, retired model IDs, beta headers now required for mid-conversation tool changes
and server-side fallback. All dated within 14 days, all published. Rejected anyway, and it is the
cleaner rejection of the two: **the API is its own validator** — free, instant, authoritative, and
already in the developer's hand. A static page that guesses at a vendor rule table can only ever
be a staler copy of the error message you get for nothing. Nearest adjacent occupant, for the
record: hidekazu-konishi's Anthropic tool-use schema builder/validator.
**No revisit trigger** — "the vendor's own endpoint answers it in one call" does not expire.

### 6. CLONE lane — **empty, fourth consecutive run**

One honest sweep for a paid dev utility with corroborated current revenue plus a structural gap.
Everything surfaced was the marketing shape the lane rules exclude by construction — "$14.2k
monthly", "$10k+/month with simple apps", "0 to $300 per month" — first-person claims with no
marketplace data or independent corroboration behind them. Nothing entered the screen.
**This is now a lane-design finding, not a scouting-effort one**, and it is carried to Sunday
triage for the second time: three of the four empties trace to the same structural fact (no
inference key by design), and the fourth to the corroboration bar. Either the lane needs a
different definition of edge or it should be retired.

---

## PICK — ship 006 `npm-publish-preflight`

**Build order**

- **Page:** `public/006-npm-publish-preflight/index.html`, single self-contained file, no network
  calls, no key, no account, nothing leaves the browser (stated on the page — the input is a
  private file tree).
- **Inputs:** four boxes — `package.json`, `.npmignore`, `.gitignore`, and a file list (one
  relative path per line, as produced by `git ls-files` or `find . -type f`).
- **Output:** a per-file verdict — PUBLISHED / EXCLUDED — each carrying the one rule that decided
  it: the source (`always-included` · `always-excluded` · `files[]` · `.npmignore:12` ·
  `.gitignore:4` · `default`), the pattern text, and its line number. Plus three named alarms:
  (a) a secret-shaped file is in the tarball, (b) `main`/`bin` targets are missing from it,
  (c) no `files`, no `.npmignore` and no `.gitignore` → you are publishing your whole working tree.
- **Rules panel on the page** linking the three npm sources above, so a reader can audit the tool
  instead of trusting it.
- **Engineering — borrow, do not hand-roll.** Matcher core is `lib/nfa.js` (Thompson NFA + Pike VM,
  already proven linear-time and non-backtracking); the new piece compiles gitignore syntax onto
  it. **Every box goes through `lib/door.js` before any split** — the exact defect class that killed
  ship 003 — with `maxChars`/`maxLines`/`maxSegments`/`maxLineLen`/`maxField` all set. Output via
  `door.buffer()` (bounded by construction), every drawn cell via `door.clip()`, every
  HTML-from-input via `esc()`. Page assembled by `lib/inline.js` so the inlined copy is provably
  identical to `lib/` rather than a silent fork.
- **`lib/` contribution back:** `lib/npmignore.js` (gitignore-syntax → NFA, with `!` negation,
  trailing-`/` directory-only and leading-`/` anchoring) plus `lib/checks/npmfiles-oracle.json`
  — reference cases derived from the three cited pages, so the next ship inherits the corpus.
- **Deploy target:** branch `ship/006-npm-publish-preflight` → `public/006-npm-publish-preflight/`.
  Staged only. Publishing is Theshin's merge.
- **Distribution (non-ads):** the hub row; a drafted X post; and the organic path that is the whole
  reason this candidate ranks — every existing answer to "what will npm actually publish?" is prose,
  so the query itself is the channel. Not an MCP ship, so no registry PR.
- **Analytics:** `lib/beacon.html`, cookieless, no PII, POPIA-safe. Live token, so day-006 signal
  is measurable from the first visit.
- **Kill-criterion:** <300 per-path visits to `/006-npm-publish-preflight/` in 30 days → graveyard.

**ANTI-GRIND CLAUSE — written now, before the build, binding on this run.** Ship 003 died on a
pattern: every fix cycle closed one defect and revealed its sibling, three days running. So:
this build gets **one fix cycle and one targeted re-check, and no second day.** If it has not
reached a checker PASS by then it stays an unmerged FAIL with the residual named, or it is killed
— it does not come back tomorrow as a rebuild. Written before any verdict exists, so the decision
cannot be made by wanting the ship.

**Sibling-sweep rule (inherited, `lib/checks/door-limits.json`, earned 2026-08-06):** when a fix
closes a defect, sweep **every sibling call site of the same shape in the same commit**. Two of
the last three FAILs were a sibling of the thing just fixed.

**Deliberate deviation, disclosed (same as 2026-08-06):** step-B's brief notification is **not**
sent. It carries no decision — a 04:45 SAST push for "here is what I am about to build" spends
attention for nothing. One notification goes out, at the terminal state, where a decision exists.
Flagged rather than skipped silently.
